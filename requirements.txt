# Core data and numerical libraries
numpy>=1.26
pandas>=2.2
requests>=2.32
python-dotenv>=1.0
certifi

# Market‑data helpers
yfinance>=0.2.38
pandas_datareader>=0.10
polygon-api-client>=1.14.0  # simple REST wrapper

# Machine‑learning / modelling
torch      # Let pip pick compatible version for your Python (avoid == unless needed)
transformers>=4.41

# Signal Generator / Sentiment Analysis
newsapi-python>=0.2.7
gnews>=0.2.6
pytrends>=4.9.2
snscrape>=0.7.0
praw>=7.8.1

# Volatility modeling
arch>=6.3

# Advanced risk management dependencies
scipy>=1.9.0

# Visualisation
streamlit

# Optional broker/IB integration (planned)
ib-insync>=0.9.86

# Linting / testing (dev only)
pytest
ruff
black

# Logging (in case you use external logging packages in the future)
# loguru

# OpenAI client (for sentiment)
openai>=1.0.0

